from flask_sqlalchemy import SQLAlchemy

db = SQLAlchemy() #object that will link to our flask app and its going to look at allthe object() we tell it to and its going to map those objects to rows in a DB
